Obstacle_North		= (OR 	(OR		(D DistanceNorthRight VERYNEAR)
									(D DistanceNorthLeft VERYNEAR))
							(OR		(D DistanceNorthRight TOOCLOSE)
									(D DistanceNorthLeft TOOCLOSE)));
Obstacle_East	    = (OR	(D DistanceEast VERYNEAR)	(D DistanceEast TOOCLOSE));
Obstacle_West       = (OR	(D DistanceWest VERYNEAR)	(D DistanceWest TOOCLOSE));

Near_North			= (OR	(D DistanceNorthRight NEAR)
							(D DistanceNorthLeft NEAR));
Far_North			= (AND	(D DistanceNorthRight FAR)
							(D DistanceNorthLeft FAR));
VeryFar_North		= (AND	(D DistanceNorthRight VERYFAR)
							(D DistanceNorthLeft VERYFAR));

Random_Left			= (D Random LEFT);
Random_Right		= (D Random RIGHT);
Priority_Left		= (D Priority LEFT);
Priority_Right		= (D Priority RIGHT);

E_KSONE				= (D DistanceEast KSONE);
E_KSTWO				= (D DistanceEast KSTWO);
E_KSTHREE			= (D DistanceEast KSTHREE);
E_KSFOUR			= (D DistanceEast KSFOUR);

W_KSONE				= (D DistanceWest KSONE);
W_KSTWO				= (D DistanceWest KSTWO);
W_KSTHREE			= (D DistanceWest KSTHREE);
W_KSFOUR			= (D DistanceWest KSFOUR);

GhostFound			= (D GhostFound TRUE);
Escaping_From_Ghost = (D EscapingFromGhost TRUE);
Ghost_Near			= (D GhostBlobHeight BIG);
Is_SuperMode		= (D SuperMode TRUE);
RFIDPriority_Left	= (D RFIDPriority LEFT);
RFIDPriority_Right	= (D RFIDPriority RIGHT);
RFIDPriority_Backwards	= (D RFIDPriority BACKWARDS);